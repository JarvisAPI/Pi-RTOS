Minimize
    number of bins used
Subject To
    use at least 1 bin
    sum of all items in any particular bin is less than the capacity of said bin
    each item goes into exactly one bin
Bounds
    each bin is either used or not used
    each item is either placed or not placed into a particular bin
Semis
    bins, items
End
